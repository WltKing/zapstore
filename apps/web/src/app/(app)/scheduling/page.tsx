import { PagePlaceholder } from "@/components/page-placeholder";

export default function SchedulingPage() {
  return (
    <PagePlaceholder
      title="Agendamentos"
      description="Profissionais, serviços e agenda de horários. Para salão, barbearia, clínica."
      layer="Camada 1"
    />
  );
}
