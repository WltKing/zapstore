import { PagePlaceholder } from "@/components/page-placeholder";

export default function CustomersPage() {
  return (
    <PagePlaceholder
      title="Clientes"
      description="Cadastro de clientes, histórico de compras e contatos."
      layer="Camada 1"
    />
  );
}
