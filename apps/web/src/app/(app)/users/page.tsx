import { PagePlaceholder } from "@/components/page-placeholder";

export default function UsersPage() {
  return (
    <PagePlaceholder
      title="Usuários"
      description="Equipe da loja com perfis (administrador, operador, entregador)."
      layer="Camada 1"
    />
  );
}
