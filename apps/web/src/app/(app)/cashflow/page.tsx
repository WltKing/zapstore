import { PagePlaceholder } from "@/components/page-placeholder";

export default function CashflowPage() {
  return (
    <PagePlaceholder
      title="Caixa"
      description="Entradas (vendas) e saídas (despesas) do período, resultado do dia."
      layer="Camada 1"
    />
  );
}
