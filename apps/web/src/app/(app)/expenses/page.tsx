import { PagePlaceholder } from "@/components/page-placeholder";

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Despesas"
      description="Lançamento de despesas por categoria e forma de pagamento."
      layer="Camada 1"
    />
  );
}
