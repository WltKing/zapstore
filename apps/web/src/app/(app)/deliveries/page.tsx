import { PagePlaceholder } from "@/components/page-placeholder";

export default function DeliveriesPage() {
  return (
    <PagePlaceholder
      title="Entregas"
      description="Lista de entregas, status e capacidade por turno."
      layer="Camada 1"
    />
  );
}
